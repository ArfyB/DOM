const myName = "Max";

if (myName === "Max") {
  console.log("Hello!");
}

let isLoggedIn = true;

if(!isLoggedIn) {
  console.log('User is NOT logged in!');
}

const enterdUserName = 'Maximilan'; // empty string or 0 = false

// if(enterdUserName) == if(enterdUserName > 0)
if (enterdUserName) {
  console.log('Input is valid!')
}